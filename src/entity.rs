extern crate cgmath;
/*
pub struct Entity {
    x: f32,
    y: f32,
    width: f32,
    height: f32,
    texture_path: &'static str,
}

pub trait Model {
    fn new(&self);
}

impl Model for Entity {
    fn new(&mut self) {
        self.x+=1.0;
    }
}
*/